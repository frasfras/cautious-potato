# Racing Coach Chat - Environment Variables
# Copy this file to .env.local and fill in your values

# Optional: Pre-configure API provider (openai or synthetic)
# If not set, users can select in the UI
# REACT_APP_DEFAULT_API_PROVIDER=synthetic

# Optional: Pre-configure API key
# If not set, users must enter their key in the UI
# REACT_APP_API_KEY=your_api_key_here

# Note: For security, never commit .env.local to git
# For Netlify deployment, set these as environment variables in the Netlify dashboard
